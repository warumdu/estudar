{
  "name": "estudar",
  "version": "0.2.2",
  "private": true,
  "description": "Offline-Karteikarten-App (PWA) für brasilianisches Portugiesisch. Kein Build-Schritt: die Dateien im Repo werden von GitHub Pages unverändert ausgeliefert. package.json dient nur dem Pinnen der nach vendor/ kopierten Bibliotheken und den Node-Tests in der VM.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "vendor": "node scripts/vendor.js",
    "icons": "node scripts/make-icons.js"
  },
  "dependencies": {
    "ts-fsrs": "5.4.2"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  },
  "engines": {
    "node": ">=22"
  }
}
